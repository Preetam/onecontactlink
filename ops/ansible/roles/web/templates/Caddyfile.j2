api.onecontact.link {
	proxy /v1 localhost:4002
	header / -Server
}

onecontact.link {
	redir https://www.onecontact.link{uri}
	header / -Server
}

www.onecontact.link {
	proxy / localhost:4003
	header / -Server
}
